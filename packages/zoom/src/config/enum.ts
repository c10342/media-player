export enum ClassNameEnum {
  ZOOMRELATIVE = "zoom-relative",
  ZOOMDRAG = "zoom-drag"
}

export enum CursorEnum {
  default = "se-resize",
  x = "e-resize",
  y = "n-resize"
}

export enum CustomEvents {
  ZOOM = "zoom"
}
